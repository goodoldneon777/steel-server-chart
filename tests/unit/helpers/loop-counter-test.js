import { loopCounter } from '../../../helpers/loop-counter';
import { module, test } from 'qunit';

module('Unit | Helper | loop counter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = loopCounter(42);
  assert.ok(result);
});
