{{field-choose-select
	area = area
	selects = fieldChooseSelect
	name = "category"
	filterToggle = (action 'filterToggle')
	feed = (action 'feed')}}

{{#if filterFlag}}
	{{field-choose-filter
		filters = fieldChooseFilter}}
{{/if}}